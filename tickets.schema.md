# tickets.json

Every ticket must include:
- `why` (never blank)
- `wrong_if`
- `pair_url` (Dex)
- `mint`

On CLOSE also:
- `exit_url` (or same as `pair_url`)

DEV shows why + chart from pair_url / exit_url.
